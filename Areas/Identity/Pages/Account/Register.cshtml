@page
@model RegisterModel
@{
    ViewData["Title"] = "Register";
}

<div class="max-w-5xl mx-auto px-4 py-10">

    <h1 class="text-3xl font-bold tracking-tight mb-8">@ViewData["Title"]</h1>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-start">

        <!-- Register form -->
        <div>
            <form id="registerForm" asp-route-returnUrl="@Model.ReturnUrl" method="post">
                <h2 class="text-xl font-semibold mb-1">Create a new account</h2>
                <hr class="border-gray-200 mb-6" />

                <div asp-validation-summary="ModelOnly" class="mb-4 p-3 rounded-lg border border-red-200 bg-red-50 text-sm text-red-600" role="alert"></div>

                <!-- Email -->
                <div class="mb-4">
                    <label asp-for="Input.Email" class="block text-sm font-semibold text-gray-800 mb-2">Email</label>
                    <input asp-for="Input.Email" autocomplete="username" aria-required="true" placeholder="name@example.com"
                           class="block w-full border border-gray-300 rounded-lg px-3 py-2 text-sm text-gray-900 placeholder-gray-300 outline-none focus:border-gray-900 transition-colors" />
                    <span asp-validation-for="Input.Email" class="mt-1 block text-xs text-red-600"></span>
                </div>

                <!-- Username -->
                <div class="mb-4">
                    <label asp-for="Input.UserName" class="block text-sm font-semibold text-gray-800 mb-2">Username</label>
                    <input asp-for="Input.UserName" aria-required="true" placeholder="Username"
                           class="block w-full border border-gray-300 rounded-lg px-3 py-2 text-sm text-gray-900 placeholder-gray-300 outline-none focus:border-gray-900 transition-colors" />
                    <span asp-validation-for="Input.UserName" class="mt-1 block text-xs text-red-600"></span>
                </div>

                <!-- Password -->
                <div class="mb-4">
                    <label asp-for="Input.Password" class="block text-sm font-semibold text-gray-800 mb-2">Password</label>
                    <input asp-for="Input.Password" autocomplete="new-password" aria-required="true" placeholder="Password"
                           class="block w-full border border-gray-300 rounded-lg px-3 py-2 text-sm text-gray-900 placeholder-gray-300 outline-none focus:border-gray-900 transition-colors" />
                    <span asp-validation-for="Input.Password" class="mt-1 block text-xs text-red-600"></span>
                </div>

                <!-- Confirm password -->
                <div class="mb-6">
                    <label asp-for="Input.ConfirmPassword" class="block text-sm font-semibold text-gray-800 mb-2">Confirm Password</label>
                    <input asp-for="Input.ConfirmPassword" autocomplete="new-password" aria-required="true" placeholder="Confirm password"
                           class="block w-full border border-gray-300 rounded-lg px-3 py-2 text-sm text-gray-900 placeholder-gray-300 outline-none focus:border-gray-900 transition-colors" />
                    <span asp-validation-for="Input.ConfirmPassword" class="mt-1 block text-xs text-red-600"></span>
                </div>

                <button id="registerSubmit" type="submit"
                        class="w-full inline-flex items-center justify-center px-5 py-2.5 rounded-lg bg-gray-900 text-white text-sm font-semibold hover:bg-gray-700 transition-colors">
                    Register
                </button>
            </form>
        </div>

        <!-- External services -->
        <div>
            <section>
                <h3 class="text-lg font-semibold mb-1">Use another service to register</h3>
                <hr class="border-gray-200 mb-6" />

                @{
                    if ((Model.ExternalLogins?.Count ?? 0) == 0)
                    {
                        <div class="text-sm text-gray-500 leading-relaxed">
                            <p>
                                There are no external authentication services configured. See this
                                <a href="https://go.microsoft.com/fwlink/?LinkID=532715" class="text-gray-900 underline hover:text-gray-700 transition-colors">
                                    article
                                    about setting up this ASP.NET application to support logging in via external services
                                </a>.
                            </p>
                        </div>
                    }
                    else
                    {
                        <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post">
                            <div class="flex flex-wrap gap-2">
                                @foreach (var provider in Model.ExternalLogins!)
                                {
                                    <button type="submit" name="provider" value="@provider.Name"
                                            title="Log in using your @provider.DisplayName account"
                                            class="inline-flex items-center px-4 py-2 rounded-lg border border-gray-300 text-sm text-gray-700 bg-white hover:border-gray-900 hover:text-gray-900 transition-colors">
                                        @provider.DisplayName
                                    </button>
                                }
                            </div>
                        </form>
                    }
                }
            </section>
        </div>

    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}