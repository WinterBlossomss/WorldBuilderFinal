@model IEnumerable<WorldBuilder.Models.Genre>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GenreDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Worlds)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.GenreDescription)
            </td>
            <td>
                @Html.DisplayFor(model => item.Worlds)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GenreName">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GenreName">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GenreName">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
